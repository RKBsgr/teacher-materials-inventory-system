# Additional Task: Password Visibility Toggle in Landing

## Steps:
- [x] 1. Add `showPassword` state in Landing.jsx.
- [x] 2. Replace password input with wrapper div containing input (type={showPassword ? 'text' : 'password'}) and eye button.
- [x] 3. Add togglePassword function.
- [x] 4. Add CSS for password-wrapper, eye-icon (using Unicode or SVG).
- [x] 5. Add `autoComplete="new-password"` to prevent autofill.
- [x] 6. Complete.

